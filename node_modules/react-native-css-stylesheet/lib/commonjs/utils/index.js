"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.regexTest = exports.isValidPositiveNumber = exports.isNumber = void 0;

const regexTest = (regex, str) => {
  const results = [];
  const m = regex.exec(str);

  if (m !== null) {
    m.forEach(match => {
      if (match && match.length > 0) {
        results.push(match.trim());
      }
    });
  }

  return results;
};

exports.regexTest = regexTest;
const numberRegex = /^\d+(?:\.\d+)?$/;

const isNumber = number => regexTest(numberRegex, number).length > 0;

exports.isNumber = isNumber;

const isValidPositiveNumber = (number, notNumberMsg, shouldBePositiveMsg) => {
  let formattedNumber = number;

  if (!isNumber(formattedNumber)) {
    throw new Error(`${notNumberMsg}\n Found: ${formattedNumber}`);
  } else {
    formattedNumber = parseFloat(formattedNumber);
  }

  if (formattedNumber <= 0) {
    throw new Error(`${shouldBePositiveMsg || notNumberMsg}\n Found: ${formattedNumber}`);
  }

  return formattedNumber;
};

exports.isValidPositiveNumber = isValidPositiveNumber;
//# sourceMappingURL=index.js.map