import { useEffect, useState } from "react";
import { useWindowDimensions } from "react-native";
export default function useCssStyleSheet(responsiveStyles) {
  const window = useWindowDimensions();
  const [newStyles, setNewStyles] = useState(responsiveStyles());
  useEffect(() => {
    setNewStyles(responsiveStyles());
  }, [window, responsiveStyles]);
  return newStyles;
}
//# sourceMappingURL=useCssStyleSheet.js.map